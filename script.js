"use strict";

// DOM (Document Object Model) MANIPULATION : Its when we use JavaScript to interact with browser, treating documents (html,xml) as objects.

// Select element from html:
document.querySelector(".message").textContent; // .var (select class); #var (select id) + return the textContent propertie from .message
